#include <iostream>

void mystery(int n) {
    if (n <= 1) return;

    // Два рекурсивні виклики: 2 * T(n/2)
    mystery(n / 2);
    mystery(n / 2);

    // Лінійний цикл: O(n)
    for (int i = 0; i < n; ++i) {
        // O(1) операція
    }
}

/*
Рекурентне співвідношення: T(n) = 2T(n/2) + O(n)
Аналог: Merge Sort
Складність: O(n log n)
*/
